{
  // Typecheck for Node-side tooling: tests, the D1/node:sqlite adapter, and the
  // parity harness. Uses Node types instead of (only) the Workers runtime types.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "types": ["node", "@cloudflare/workers-types"],
    "noEmit": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false
  },
  "include": ["test/**/*.ts", "scripts/**/*.ts", "src/**/*.ts"]
}
